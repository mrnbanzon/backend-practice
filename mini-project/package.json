{
  "name": "mini-project",
  "version": "1.0.0",
  "description": "mini project that is packaged into a Docker image and deployed to AWS",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "./src/server.js",
  "scripts": {
    "test": "jest",
    "start": "node ./src/server.js",
    "dev": "node --env-file=.env --watch ./src/server.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "jest": "^30.5.1"
  }
}
