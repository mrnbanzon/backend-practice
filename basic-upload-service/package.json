{
  "name": "basic-upload-service",
  "version": "1.0.0",
  "description": "basic upload service that accepts file uploads and saves them as gzip files on disk",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
